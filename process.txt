python -m venv .airline_chatbot
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope Process
.airline_chatbot\Scripts\activate

# 2. Upgrade pip and install the Golden Set
python -m pip install --upgrade pip
pip install -r requirements.txt

Step 1 : 
    # run api_server.py in a new terminal
    .airline_chatbot\Scripts\activate
    python .\api_server.py

    # api server will start on 8001 port 

Step 2 : create new terminal 
    python main.py

